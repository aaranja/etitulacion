class ValidationFiles():
	def constructor(self):
		super(self)

	def do_validation(self, data):
		print(data)

		# extract the files and validate attrs
		# the file names are:
		# file_acta, file_bach, file_curp, file_ingles, file_lic, file_ssoc

		documents = data['documents']

		# for doc, attr in documents.items():


		# file_acta = data['file_acta']
		# file_bach = data['file_bach']
		# file_curp = data['file_curp']
		# file_ingles = data['file_ingles']
		# file_lic = data['file_lic']
		# file_ssoc = data['file_ssoc']

		


		return "IS OK"